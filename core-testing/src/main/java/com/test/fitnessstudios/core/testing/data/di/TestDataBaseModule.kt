package com.test.fitnessstudios.core.testing.data.di

/*@Module
@InstallIn(SingletonComponent::class)
object TestDataBaseModule {

    /*@Provides
    @Singleton
    fun provideTestAppDatabase(app: Application): AppDatabase {
        return Room.inMemoryDatabaseBuilder(
            app,
            AppDatabase::class.java,
        ).build()
    }

    @Provides
    @Singleton
    fun provideFitnessStudioRepository(db: AppDatabase): FitnessStudioRepository {
        return DefaultFitnessStudioRepository(db.fitnessStudioDao())
    }*/

    /*@Provides
    @Singleton
    fun provideNoteUseCases(repository: FitnessStudioRepository): FitnessUseCase {
        return FitnessUseCase(
            getNotes = GetNotes(repository),
            deleteNote = DeleteNote(repository),
            addNote = AddNote(repository),
            getNote = GetNote(repository)
        )
    }*/
}
*/
